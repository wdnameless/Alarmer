# REQUIREMENTS MANIFEST: Reliability and Product Depth

Аудит: `bun run check` (59 тестов) и `cargo test` (8 тестов) были зелёными,
но flagship-функция в состоянии по умолчанию не работала. Причина структурная:
тестовое окружение — `node`, компонентных тестов нет, поэтому дефект жизненного
цикла React не мог быть пойман.

| ID | Requirement | Source (verbatim) | Status | Phase |
|---|---|---|---|---|
| R01 | Будильник звонит на любом экране, включая дефолтный | "будильники не звонят" — `Alarms.tsx:85` слушатель, `DashboardView.tsx:101` монтирование | done | 2 |
| R02 | «Остановить» действительно останавливает | "кнопка не останавливает" — `scheduler.rs:81` снимал fired-маркер | done | 2 |
| R03 | Таймер переживает смену экрана и скрытие окна | "таймер умирает при переключении" — `DashboardView.tsx:100` условный монтаж | done | 3 |
| R04 | Одна семантика `days: []` | три источника: `types/index.ts:84` once, `ai.ts:72` один раз, `scheduler.rs:196` каждый день | done | 2 |
| R05 | Пропущенные срабатывания не теряются молча | `snoozed` в памяти, выход = потеря | done | 2 |
| R06 | Ошибки ИИ видимы, а не подменяются офлайн-ответом | CSP `connect-src` фиксирован, `catch` → тихий откат | done | 5 |
| R07 | API-ключ не в файле экспорта | key лежал в `alarmer.json` | done | 5 |
| R08 | Будильник слышен при скрытом окне | WebView2 суспендит страницу, звука нет | done | 4 |
| R09 | Задачи: «что делать», а не только «когда» | расписание отвечает «когда», не «что» | done | 6 |
| R10 | Статистика из реально отработанных сессий | `BlockPlayer` мерил интервалы и выбрасывал результат | done | 6 |
| R11 | Следующий такой дефект ловится гейтом | vitest `environment: node`, CI нет | done | 1 |
| R12 | Мёртвые файлы, зависимости и типы удалены | `App.css`, SVG шаблона, `canvas`, `AppSettings` | done | 8 |
| R13 | Темы доступны пользователю | `themeKey` зафиксирован `'winter'`, сеттера нет | done | 7 |
