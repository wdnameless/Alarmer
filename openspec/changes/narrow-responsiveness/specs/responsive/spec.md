## ADDED Requirements

### Requirement: Compact Responsive Navigation
Navigation bars and selectors MUST dynamically adapt to narrow window sizes without text truncation, horizontal scroll, or element clipping.

#### Scenario: Narrow window navigation
- **WHEN** the window width is reduced below 320px
- **THEN** navigation tabs collapse to icons with tooltips, maintaining full accessibility without overflow.
