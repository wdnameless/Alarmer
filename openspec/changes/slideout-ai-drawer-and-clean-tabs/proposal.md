# Change: Dedicated Slide-Out / Detached AI Drawer & Clean Dual Navigation

## Why
1. User requests removing "AI Co-Pilot" from the center tab bar and removing the bot icon from the top titlebar.
2. The tab bar should only have "Дашборд" and "Настройки".
3. A sleek floating/side button on the edge of the window will toggle an animated slide-out side panel (drawer) for the AI conversation, keeping the timer visible while chatting with the AI.

## Verification
- Center navigation has only 2 tabs: Дашборд and Настройки.
- Top title bar has no bot icon.
- A neat side trigger button (e.g. on the right edge or compact floating pill) smoothly slides out the AI panel.
- Writing to AI in the slide-out panel works seamlessly, sets alarms/timers and preserves chat history.
